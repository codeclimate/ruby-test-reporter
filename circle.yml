dependencies:
  pre:
    - git config --global user.email "ci@codeclimate.com"
    - git config --global user.name "Code Climate CI"
  override:
    - echo "skip"

test:
  override:
  - SIMPLECOV_VERSION="<= 0.13" bin/ci
  - SIMPLECOV_VERSION=">= 0.14" bin/ci
